# Debian/Ubuntu VirtualHost для sites-available/app.conf.
# Включить a2ensite только после apachectl configtest; затем reload apache2.
<VirtualHost *:80>
    ServerName app.example.test
    DocumentRoot /srv/www/app
    # Package variable помещает logs в системный Apache log directory.
    ErrorLog ${APACHE_LOG_DIR}/app-error.log
    CustomLog ${APACHE_LOG_DIR}/app-access.log combined
    <Directory /srv/www/app>
        # Явно разрешаем чтение document root и запрещаем .htaccess overrides.
        AllowOverride None
        Require all granted
    </Directory>
</VirtualHost>
