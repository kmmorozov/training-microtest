; Полный /etc/php/8.3/fpm/php-fpm.conf для выделенной учебной ВМ Ubuntu 24.04 (8.3); Debian замените версию во всех путях.
; Встроенный pool заменяет /etc/php/8.3/fpm/pool.d/www.conf: внешних include здесь нет.
[global]
; systemd получает сообщения с stderr; master остается в foreground.
pid = /run/php/php8.3-fpm.pid
error_log = /proc/self/fd/2
daemonize = no

[www]
; Один пользователь согласован с www-data.conf и владельцем uploads.
user = www-data
group = www-data
listen = /run/php/php8.3-fpm.sock
; listen.acl_users намеренно не задается: иначе он перекроет owner/group.
listen.owner = www-data
listen.group = www-data
listen.mode = 0660
; ondemand экономит память; максимум пять рабочих процессов на учебной ВМ.
pm = ondemand
pm.max_children = 5
pm.process_idle_timeout = 10s
pm.max_requests = 500
; Не передавать приложению окружение сервиса и не исполнять другие расширения.
clear_env = yes
security.limit_extensions = .php
catch_workers_output = yes
php_admin_flag[log_errors] = on
php_admin_value[error_log] = /proc/self/fd/2
; Ограничение uploads согласуйте с client_max_body_size Nginx.
php_admin_value[upload_max_filesize] = 16M
php_admin_value[post_max_size] = 20M
