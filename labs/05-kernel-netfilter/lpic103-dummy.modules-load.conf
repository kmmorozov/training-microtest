# Установить как /etc/modules-load.d/lpic103-dummy.conf.
# Одна строка имени просит systemd-modules-load загрузить dummy при старте.
# Параметры берутся из отдельного файла /etc/modprobe.d/lpic103-dummy.conf.
dummy
