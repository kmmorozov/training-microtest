# Установить как /etc/sysctl.d/60-lpic103-openvpn.conf.
# IPv4 forwarding нужен для маршрута VPN client → lab LAN; он не открывает доступ
# сам по себе — поток дополнительно ограничивается firewall policy.
net.ipv4.ip_forward = 1
