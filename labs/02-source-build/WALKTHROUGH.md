# Лабораторная работа 02. Контролируемая сборка iperf3 из исходного кода

[Подготовка, пакеты, полные конфиги и уточнения](README.md) · [Общие материалы и ключи](../../PREPARATION.md)

Ниже все шаги руководства лабораторных v4 от 10.08.2026 с сохраненными
командами и ожидаемыми результатами. Сначала выполните подготовку в README:
она определяет адреса, значения переменных и различия ОС. Команды выполняются
по одной, на указанной машине; альтернативные ветки ОС не выполняются вместе.
В командах по умолчанию X=10/Y=20/Z=30, lab NIC=enp0s8, зоны internal/external/public.
Их необходимо сопоставить с реальными NIC/zone по README. Имена unit и web-user
по умолчанию Rocky; для Debian/Ubuntu используйте соответствия из README.
Выводы — образцы, а не протокол запуска на вашей ВМ.

Цель лабораторной работы: пройти полный цикл доверенной сборки: проверка архива, зависимости, configure, test и установка в /usr/local.

Стенд: одна Linux-ВМ с доступом к репозиторию пакетов; архив iperf3-X.Y.tar.gz и SHA256SUMS предоставляет преподаватель.

Сценарий: в репозитории нет требуемой версии iperf3. Слушатель собирает выданный преподавателем архив, проверяет его целостность и устанавливает программу в /usr/local.

Планируемый результат: собранная версия iperf3 запускается из /usr/local/bin, проходит локальный client/server-тест, а файлы сборки не перезаписывают системный пакет.

## Ограничения и безопасность

Важно: не запускайте make install, если checksum или make check завершились ошибкой.

Важно: не используйте prefix /usr: этим каталогом управляет пакетный менеджер.

## Ход выполнения

## Шаг 1. Установить требуемые пакеты

**Зачем: Установите компилятор, make и заголовки OpenSSL из репозитория своей ОС. Ветки команд не смешиваются. Установите пакеты только для выбранного семейства ОС. Ключ -y подтверждает пакетную транзакцию без дополнительного запроса.**

```bash
# RHEL-like
sudo dnf group install -y 'Development Tools'
```

## Шаг 2. Установить требуемые пакеты

**Зачем: Устанавливаем заголовки и библиотеки разработки OpenSSL, необходимые для конфигурации и сборки iperf3 в RHEL-подобной ОС.**

```bash
sudo dnf install -y openssl-devel
```

## Шаг 3. Обновить метаданные пакетов

**Зачем: Обновите локальные метаданные репозиториев до установки пакетов; это действие не устанавливает и не обновляет сами пакеты.**

```bash
# Debian/Ubuntu
sudo apt update
```

## Шаг 4. Установить требуемые пакеты

**Зачем: Устанавливаем в Debian/Ubuntu компилятор, средства сборки и заголовки OpenSSL, необходимые для сборки iperf3 из исходников.**

```bash
sudo apt install -y build-essential pkg-config libssl-dev
```

cc --version и make --version возвращают версии.

## Шаг 5. Создать каталог с заданными правами

**Зачем: Перед распаковкой сверьте имя и checksum с переданным манифестом. Команда install создает каталог или копирует файл сразу с заданными владельцем и mode: -d выбирает каталог, -m задает права, -o/-g — владельца и группу.**

```bash
install -d -m 0750 ~/src/lpic103-iperf3
```

## Шаг 6. Скопировать файл или каталог

**Зачем: Копируем скачанный архив в рабочий каталог сборки, чтобы исходные материалы оставались отдельно от распакованных и собранных файлов.**

```bash
cp "$REPO/generated/downloads/iperf"/iperf-3.21.tar.gz ~/src/lpic103-iperf3/
```

## Шаг 7. Скопировать файл или каталог

**Зачем: Переносим манифест контрольных сумм рядом с архивом, чтобы проверить целостность полученных материалов перед распаковкой.**

```bash
cp "$REPO/generated/downloads/iperf"/SHA256SUMS ~/src/lpic103-iperf3/
```

## Шаг 8. Перейти в рабочий каталог

**Зачем: Переходим в каталог архива и манифеста, чтобы относительные имена в проверке SHA256SUMS разрешались правильно.**

```bash
cd ~/src/lpic103-iperf3
```

## Шаг 9. Проверить контрольную сумму

**Зачем: Сверяем архив с манифестом: несовпадение означает, что распаковку и сборку нужно остановить и проверить источник файлов.**

```bash
sha256sum -c SHA256SUMS
```

Ожидаемый вывод и результат:

```text
<файл>: OK
```

Для архива получено OK.

## Шаг 10. Обработать архив

**Зачем: Распаковываем проверенный архив, чтобы получить исходники, configure и тесты программы.**

```bash
tar xf iperf-3.21.tar.gz
```

## Шаг 11. Перейти в рабочий каталог

**Зачем: Переходим в распакованное дерево исходников, где находятся configure, тесты и файлы сборки.**

```bash
cd iperf-3.*
```

## Шаг 12. Подготовить сборку

**Зачем: Проверяем зависимости и создаем файлы сборки с установочным префиксом /usr/local, отделяя результат от пакетного ПО.**

```bash
./configure --prefix=/usr/local
```

Ожидаемый вывод и результат:

```text
config.status: creating Makefile
Configuration completed; exit status = 0
```

## Шаг 13. Выполнить этап сборки

**Зачем: Компилируем программу с параллелизмом по числу процессоров, чтобы получить исполняемые файлы без установки в систему.**

```bash
make -j"$(nproc)"
```

Ожидаемый вывод и результат:

```text
<сообщения компиляции>; exit status = 0
```

## Шаг 14. Выполнить этап сборки

**Зачем: Запускаем тесты собранной программы до установки, чтобы выявить ошибки сборки или несовместимость окружения.**

```bash
make check
```

Ожидаемый вывод и результат:

```text
Testsuite summary ...
TOTAL: <N>  PASS: <N>  FAIL: 0
```

make check завершился с exit code 0.

config.log сохранен для последующего анализа.

## Шаг 15. Выполнить этап сборки

**Зачем: Только после тестов установите файлы в /usr/local, обновите cache библиотек и проведите локальный тест. Выполните один этап сборки или проверки зависимостей и переходите дальше только при нулевом exit status.**

```bash
sudo make install
```

Ожидаемый вывод и результат:

```text
<сообщения компиляции>; exit status = 0
```

## Шаг 16. Обновить кэш динамических библиотек

**Зачем: Обновляем кэш динамического загрузчика, чтобы установленная программа могла найти библиотеку libiperf.**

```bash
sudo ldconfig
```

## Шаг 17. Запустить или проверить iperf3

**Зачем: Проверяем запуск и версию именно установленного в /usr/local iperf3, исключая случайный запуск пакетной версии.**

```bash
/usr/local/bin/iperf3 --version
```

Ожидаемый вывод и результат:

```text
iperf 3.<версия> ...
```

## Шаг 18. Проверить библиотеки программы

**Зачем: Проверяем пути к динамическим библиотекам и отсутствие not found, чтобы исключить проблемы загрузки до сетевого теста.**

```bash
ldd /usr/local/bin/iperf3
```

Ожидаемый вывод и результат:

```text
<library> => <path> ...
Строк not found нет.
```

## Шаг 19. Запустить временный unit

**Зачем: Запускаем временный сервер на один тестовый сеанс, чтобы проверить собранную программу без создания постоянной службы.**

```bash
sudo systemd-run --unit=lpic103-iperf-server.service /usr/local/bin/iperf3 -s -1
```

Ожидаемый вывод и результат:

```text
Running as unit: <имя>.service
```

## Шаг 20. Выдержать технологическую паузу

**Зачем: Даем временному серверу время открыть порт перед подключением клиента; готовность затем подтверждается самим подключением.**

```bash
sleep 1
```

## Шаг 21. Запустить или проверить iperf3

**Зачем: Проверяем обмен между клиентом и сервером iperf3 на loopback, отделяя исправность сборки от внешней сети и firewall.**

```bash
/usr/local/bin/iperf3 -c 127.0.0.1 -t 3
```

Ожидаемый вывод и результат:

```text
[ ID] Interval Transfer Bitrate
[  5] 0.00-3.00 sec <объем> <скорость> sender/receiver
```

## Шаг 22. Просмотреть журнал службы

**Зачем: Выберите журнал нужного unit и ограничьте временной диапазон, чтобы связать сообщение с только что выполненным действием.**

```bash
sudo journalctl -u lpic103-iperf-server.service --no-pager
```

Ожидаемый вывод и результат:

```text
... <unit>[PID]: <сообщение, относящееся к текущему действию> ...
```

Клиент показывает measured throughput и exit code 0.

ldd не выводит not found.
