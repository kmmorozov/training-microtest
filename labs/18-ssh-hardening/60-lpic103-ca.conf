# Первый drop-in лабораторной 18. Установить в /etc/ssh/sshd_config.d/.
# Сервер доверяет только PUBLIC key offline user CA; private CA здесь быть не должно.
TrustedUserCAKeys /etc/ssh/lpic103_user_ca.pub
